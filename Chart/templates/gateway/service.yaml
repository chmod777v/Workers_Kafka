apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  selector:
    app: gateway
  ports:
    - port: {{ .Values.gateway.port }}             # Порт ПОДА
      targetPort: {{ .Values.gateway.port }} # Порт СЕРВЕСА внутри пода
  type: ClusterIP  #ClusterIP, NodePort, LoadBalancer