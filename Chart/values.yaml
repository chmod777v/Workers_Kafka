gateway:
  replicaCount: 1
  image: workers_kafka-gateway
  port: 8090

  probe:
    port: 8080
    path: /ready

worker:
  replicaCount: 5
  image: workers_kafka-worker

ingress:
  host: "localhost"
  path: "/"

postgres:
  username: "postgres"
  password: "1234"
  dbName: "Tasks"

