services:
  kafka-ui:
      image: provectuslabs/kafka-ui
      container_name: kafka_ui
      ports:
        - 8080:8080
      restart: always
      environment:
        - KAFKA_CLUSTERS_0_NAME=local
        - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=host.docker.internal:50936
